env:
  - GHC_VERSION=7.10.1

before_install:
 - travis_retry sudo add-apt-repository -y ppa:hvr/ghc
 - travis_retry sudo apt-get update
 - travis_retry sudo apt-get install cabal-install-1.22 ghc-${GHC_VERSION}
 - export PATH=/opt/ghc/${GHC_VERSION}/bin:/opt/cabal/1.22/bin:$PATH

install:
 - cabal --version
 - echo "$(ghc --version) [$(ghc --print-project-git-commit-id 2> /dev/null || echo '?')]"
 - travis_retry cabal update
 - cabal install --only-dependencies --enable-tests --enable-benchmarks

script:
 - cabal build

after_success: 
 - ./floop

GH_NAME=Steven Shaw:
  secure: YeOtD/iM1t8NvwIbqVmHg01CgIR0Ji8YjyAwoRULwIW+pLzlLFzcOBxB0uGxG4rNnm01j32jcMhmvgbs1Dow3AXUlf9Eq1V4SUlE9dpzHpb49snGomxvl/XPyTNvVZP6YbEKddUv3IevtedRL5s/UnCv2imXe4UAO5aVSM4BrU4=
