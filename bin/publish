#!/usr/bin/env bash

set -eu
${DEBUG:-}

#
# Make talks.
#
for dir in talks/*; do
  [[ -d ${dir} && ${dir} != talks/bin && ${dir} != ${dir#reveal\.js} ]] && (cd ${dir} && make)
done

#
# Build and deploy to GHP.
#
bin/build && bin/site-rebuild && bin/deploy-to-github-pages
